import { StyleSheet, Text, View } from "react-native";
import React from "react";

const FoodList = () => {
  return (
    <View>
      <Text>FoodList View!</Text>
    </View>
  );
};

export default FoodList;

const styles = StyleSheet.create({});
